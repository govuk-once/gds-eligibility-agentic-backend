## Goals

* Assert that the following specifications:
  * ./skilled_worker_visa_eligibility.json
  * ./child_benefit_eligibility.json
  conform to the following schema:
  * ./eligibility-schema.json
* If the schema is not sufficient to describe the specification, increment the schema
